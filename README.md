# Arcane

A high-performance, hash-bucketed database engine designed for extreme throughput on concurrent reads and writes.

Arcane can be embedded as a library (like SQLite), or run as a standalone server.
